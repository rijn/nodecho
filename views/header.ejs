<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title><%= globals.blogName %></title>
    <meta name="description" content="<%= globals.description  %>" />
    <meta name="HandheldFriendly" content="True" />
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no, minimal-ui">
    <meta name="viewport" content="initial-scale=1.0,user-scalable=no,maximum-scale=1" />
    <link rel="shortcut icon" href="/favicon.ico">
    <link rel='stylesheet' href='/stylesheets/style.min.css' />
    <link rel='stylesheet' href='/stylesheets/font-awesome.min.css' />
  </head>
  <body>
  	<header class="page-header <%= !!thinHeader ? 'active':''%>">
  		<div class="inner">
  			<a class="link" href="/">
          <% if(!!globals.useLogo){ %>
            <img src="/images/<%= globals.blogLogo %>" alt="<%= globals.blogName %>" class="blog-logo" style="margin: 0px 0 -8px 0"/></a>
          <% } else { %>
            <%= globals.blogName %>
          <% } %>
        <small>
        <%
          (router || []).forEach(
            function(data) {
        %>
          &nbsp;&middot;&nbsp;<a class="link" href="<%=data.href || ''%>"><%=data.title || ''%></a>
        <% }) %>
        </small>
        <% if(!!globals.subscribe) { %>
  			<a class="button source-button" href="<%=globals.subscribeHref || ''%>"><%=globals.subscribeTitle || 'Subscribe'%></a>
        <% } %>
  		</div>

<script>
var _hmt = _hmt || [];
(function() {
  var hm = document.createElement("script");
  hm.src = "//hm.baidu.com/hm.js?6794d92816109b7816c28a42fc54607b";
  var s = document.getElementsByTagName("script")[0]; 
  s.parentNode.insertBefore(hm, s);
})();
</script>

  	</header>