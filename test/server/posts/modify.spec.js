describe('modify posts', function () {
    // let token = { };
    // before(() => { return dropAndRegisterAndLogin().then(_token => { token = _token; }); });

    describe('should return Unauthorized 401', () => {
        xit('if did provide correct token', () => { });
    });

    xit('should return Not Found 404 if call delete and id is incorrect', () => { });
    xit('should return OK 200 if call delete and success', () => { });

    xit('should return Not Found 404 if call put and id is incorrect', () => { });
    xit('should return OK 200 if call put and success', () => { });
});
