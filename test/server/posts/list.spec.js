describe('list posts', function () {
    xit('should be able to list all posts', () => { });

    describe('should be able to', () => {
        xit('filter using offset and limit', () => { });
        xit('filter blur name', () => { });
        xit('filter use tags', () => { });
        xit('filter use date', () => { });
        xit('sort by view', () => { });
        xit('sort by date', () => { });
    });

    xit('should not list posts that have been deleted', () => { });
});
